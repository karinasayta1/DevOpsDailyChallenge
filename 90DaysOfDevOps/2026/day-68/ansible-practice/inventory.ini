[web]
web-server ansible_host=<IP address>

[app]
app-server ansible_host=<IP address>

[db]
db-server ansible_host=<IP address>

[application:children]
web
app

[all_servers:children]
application
db

[all:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=keys
